# Simple ERC721 NFT

This project provides a basic NFT contract using ERC721 with tokenURI support.

## How to Use

1. Install dependencies:
   npm install

2. Start a local blockchain:
   npx hardhat node

3. Deploy the contract:
   npx hardhat run scripts/deploy.js --network localhost
